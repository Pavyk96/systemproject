spring:
  datasource:
    url: jdbc:h2:mem:systemproject-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  thymeleaf:
    cache: false

app:
  bootstrap-admin:
    username: admin
    password: admin123
